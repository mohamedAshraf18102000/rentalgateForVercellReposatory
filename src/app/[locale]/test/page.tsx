import CarsCard from "@/app/(components)/customCards/CarsCard/CarsCard";

const page = () => {
  return (
    <div className="mt-100 mx-auto bg-red-600">
      <div className="w-[90%] bg-blue-100 mx-auto ">
        <CarsCard />
      </div>
    </div>
  );
};

export default page;

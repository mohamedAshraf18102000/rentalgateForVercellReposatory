import WrapperContainer from "@/app/(components)/wrapperContainer/WrapperContainer";

const page = async () => {
  return <WrapperContainer exceedNav>Mohamed ASHRAF</WrapperContainer>;
};

export default page;

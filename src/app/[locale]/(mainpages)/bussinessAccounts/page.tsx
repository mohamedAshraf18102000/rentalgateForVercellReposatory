import WrapperContainer from "@/app/(components)/wrapperContainer/WrapperContainer";
import BussinessAccountsContent from "./components/BussinessAccountsContent";

const page = () => {
  return (
    <WrapperContainer exceedNav>
      <BussinessAccountsContent />
    </WrapperContainer>
  );
};

export default page;

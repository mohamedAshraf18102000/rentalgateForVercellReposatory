import { Skeleton } from "@/app/(components)/ui/skeleton";

const BookingCardSkeleton = () => {
  return <Skeleton className="w-full h-[240px] rounded-2xl"></Skeleton>;
};

export default BookingCardSkeleton;
